# Python dependencies for vocoder daemon (Option B)
# Install with: pip install -r requirements-optionb.txt

# Core audio and HTTP
numpy>=1.24.0
sounddevice>=0.4.6
httpx>=0.24.0

# Optional but recommended
systemd-python>=235  # For systemd integration (optional)